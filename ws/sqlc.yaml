version: "2"
sql:
- engine: "postgresql"
  queries: "./db/queries/"
  schema: "./db/schema/"
  gen:
    go:
      package: "db"
      out: "./db/sqlc/" //salida donde se generaran los archivos de sqlc
      emit_json_tags: true